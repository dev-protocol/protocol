// tslint:disable:no-unsafe-any
const distributorFactory = artifacts.require('DistributorFactory')

contract('DistributorFactory', () => {
	describe('Create distribution', () => {
		it('Create a distribution contract')

		it('Fail to create a distribution contract from a non-owner account')
	})

	describe('Destroy', () => {
		it('Destruct this contract')

		it('Fail to destruct this contract from a non-owner account')
	})
})
